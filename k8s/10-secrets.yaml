
apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
  namespace: nexus-observability
type: Opaque
stringData:
  # In production, use SealedSecrets or Vault
  # These are placeholders
  CLICKHOUSE_USER: "default"
  CLICKHOUSE_PASSWORD: "strongpassword"
  REDIS_PASSWORD: ""
  S3_ACCESS_KEY: "AKI..."
  S3_SECRET_KEY: "secret..."
